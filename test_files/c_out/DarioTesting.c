#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char * toStringFloat(double f){
    char* c= (char*) malloc(50); //size of the number
    sprintf(c, "%g", f);
    return c;
}
char * toStringInt(int f){
    char* c= (char*) malloc(50); //size of the number
    sprintf(c, "%d", f);
    return c;
}

char * stringConcat(char * s1,char * s2){
    char* c= (char*) malloc(strlen(s1)+strlen(s2)+1);
    c[0]='\0';
    strcpy(c,s1);
    strcat(c,s2);
    return c;
}


/********************* Variable Declarations ****************/
float c = 2.2;
char d[256] = "donato";



/********************* Functions definition  *******/
char  esempio(int parametro){
char nellino[256];
return * nellino;
}
char  somma(float francesco, char *messaggio, int ciao){
char dario[256];
int frank;
int donato;
int cicciano = 2;
if(frank){
esempio(5);
}
strcpy(dario,"sono bello");
while(donato < 10){
donato = donato + 1;
}
while(!donato < 10){
char provaesame[256] = "ciao";
int nellino = 2;
printf("%d\n",donato);

}

return * dario;
}
float  somma1(char* messaggio, char* pisello){
float dario;
int gilberto;
somma(dario, (messaggio), 0);
return dario;
}
void  somma2(char* messaggio){
int dario;
int donato;
int result;
result = dario + donato;
}
char  somma3(char* messaggio){
int donato;
int dario;
return * messaggio;
}
char  somma6(char* somma6){
int dario;
return * somma6;
}
int main(){
char dario[256];
strcpy(dario,"sono bello");
}
