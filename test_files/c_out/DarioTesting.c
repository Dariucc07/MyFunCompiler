#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char * toStringFloat(double f){
    char* c= (char*) malloc(50); //size of the number
    sprintf(c, "%g", f);
    return c;
}
char * toStringInt(int f){
    char* c= (char*) malloc(50); //size of the number
    sprintf(c, "%d", f);
    return c;
}

char * stringConcat(char * s1,char * s2){
    char* c= (char*) malloc(strlen(s1)+strlen(s2)+1);
    c[0]='\0';
    strcpy(c,s1);
    strcat(c,s2);
    return c;
}


/********************* Variable Declarations ****************/
float c = 2.2;
char d[256] = "donato";



/********************* Functions definition  *******/
char  esempio(int parametro){
char nellino[256];
return * nellino;
}
char  somma(float francesco, char *messaggio, int ciao){
char dario[256];
int frank;
int donato;
if(frank){
esempio(5);
}
{
char s[256] = "ciao"; float clet = 1.2;printf("%s\n","ciao questa è una prova");

}
strcpy(dario,"sono bello");
return * dario;
}
float  somma1(char* messaggio, char* pisello){
float dario;
int gilberto;
somma(dario, (messaggio), 0);
return dario;
}
float  sommaesame(float messaggio1, char* concatenazione){

{
int clet = 2;{
int a = 1; float b = 2.3; char size[256]; float result;result = a + b + clet;
if(result > 100){
char valore[256] = "grande";strcpy(size,valore);
}
else {
{
char valore[256] = "piccola";strcpy(size,valore);
}
}

}
}
return messaggio1;
}
void  somma2(char* messaggio){
int dario;
int donato;
int result;
result = dario + donato;
}
char  somma3(char* messaggio){
int donato;
int dario;
return * messaggio;
}
char  somma6(char* somma6){
int dario;
return * somma6;
}
int main(){
char dario[256];
strcpy(dario,"sono bello");
}
